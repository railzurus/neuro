# Медитация мечты — правила обхода для поисковых роботов.
#
# Файл обязан быть реальным: SPA-фолбэк в .htaccess отдаёт index.html на
# любой несуществующий путь, и без этого файла робот получал бы на месте
# robots.txt разметку сайта.

User-agent: *
Allow: /

# Страницы заказов приватные: адрес содержит токен доступа к записи.
Disallow: /order/

# API — служебные эндпоинты, страниц для людей под ними нет.
Disallow: /api/

# Пустой шаблон приложения: он отдаётся под остальными маршрутами,
# но сам по себе содержания не имеет (см. scripts/prerender.mjs).
Disallow: /app.html

Sitemap: https://xn--80ahccnbxf5ce4bp7cvc.xn--p1ai/sitemap.xml
