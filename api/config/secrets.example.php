<?php
/**
 * ШАБЛОН секретов. Скопируйте этот файл рядом под именем `secrets.php`
 * и подставьте ваш API-ключ SpeechKit.
 *
 * ВАЖНО: `secrets.php` не коммитится в git (см. .gitignore) и загружается
 * на сервер Beget ВРУЧНУЮ через файловый менеджер / FTP — один раз.
 * Деплой его не перезапишет и не удалит.
 *
 * Где взять ключ: console.yandex.cloud → сервисный аккаунт с ролью
 * `ai.speechkit-tts.user` → «Создать API-ключ» (секрет показывается один раз).
 */

return [
    // Секретный API-ключ сервисного аккаунта (НЕ идентификатор ключа):
    'speechkit_api_key' => 'ВАШ_API_КЛЮЧ_СЕРВИСНОГО_АККАУНТА',

    // ID каталога (folder) в Yandex Cloud:
    'folder_id' => 'b1g9p4v6cpof4g3dvpo0',
];
